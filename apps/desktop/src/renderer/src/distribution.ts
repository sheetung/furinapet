// Renderer-side mirror of the host distribution policy. Keep this pure so it
// stays inside the renderer TypeScript root and cannot pull main-process code.
export const furinaOnlyDistribution = true;
